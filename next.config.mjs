/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: false,
    images: {
      domains: ['github-readme-activity-graph.vercel.app' , "assets.leetcode.com"],
    },
};

export default nextConfig;
